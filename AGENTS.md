# 專案交接：形式原理視覺實驗室

## 專案與對象

- 專案：形式原理視覺實驗室
- 對象：七年級視覺藝術學生
- 技術：原生 HTML／CSS／JavaScript，測試使用 Node.js `node:test`

## 十個形式原理

反覆、漸層、對稱、均衡、對比、律動、比例、統一、調和、單純。

## 不可任意改動的產品邊界

- 自由複習不計分，不寫入正式成績或 unlock 狀態。
- 自由複習不得影響 LOOK／THINK／CREATE、三關 completion、Mastery 或 Classroom Control。
- 不要把自由複習做成第四關、Round 2 或正式測驗。
- 不要修改正式第三關 validator 來配合自由複習。
- 不要清理 historical cache marker 或仍在工作的 legacy route。
- 已驗收的教學內容與互動不可因重構而改變；優先小改並沿用既有 stage shell、提示區、action bar、完成流程與 success animation hook。

## 重要設計方向

- 使用 Bauhaus 多色幾何視覺：紅、黃、藍、黑及既有米白背景。畫面需有足夠色彩變化，不要只使用單色幾何圖形。
- 顏色必須服務形式原理；除非題目教的是色彩關係，否則顏色不應成為 validator 的隱藏答案。
- 每個自由複習原理使用三小題：看出原理、發現關係／問題、親自操作。三題不能只是換文字、換顏色或做極小變化。
- 每題都必須具備明確教學目的、互動、完成條件、學生可理解的回饋，以及約 0.6～1.2 秒的原理專屬動畫。
- 動畫是視覺解釋，不是 generic wiggle；不使用 confetti、彩帶或粒子，也不能改掉學生完成的作品。
- 判定標準須適合七年級學生，不要求 pixel-perfect、唯一排列或工程式數值猜測。
- 錯誤回饋不得顯示 failure code、threshold、座標或內部工程語言。

## 目前進度（checkpoint）

### Git 基準

- checkpoint 前 branch：`main`
- checkpoint 前 HEAD：`e362d65 feat: add repetition and symmetry visual experiments`
- 最近基準已包含反覆與對稱自由複習實驗；漸層為更早既有母版。

### 自由複習已開放

- 漸層：既有母版，保持原行為。
- 反覆：三題流程、重複單位修復及原理動畫已完成並已提交。
- 對稱：三題流程、鏡像拖曳、容許範圍及原理動畫已完成並已提交。
- 均衡：三題皆為 Bauhaus 多色；辨識、看出偏重、水平拖曳取得非對稱穩定，三題動畫分別表現重量呼應、偏重與衰減回穩。
- 律動：三題為「看得出 → 跟得到 → 做得出」；三題皆為 Bauhaus 多色。第二題是由左至右依序點擊，第三題自由上下拖曳，僅以肉眼可見高低差做寬鬆判定。
- 統一：三題為共同形狀辨識、方向離群辨識、旋轉箭頭修復；顏色不參與判定。
- 調和：三題為鄰近色群辨識、離群色辨識、有限候選色替換；自由複習接受多個合理鄰近色，不使用完整 color picker。

### 尚未開放

- 對比
- 比例
- 單純

不要在沒有新指示時自行施工這三項。

## 本輪剛完成

- 新增並開放統一與調和各三題自由複習實驗。
- 從第一版納入 Bauhaus 多色構成及六種不同的原理專屬成功動畫。
- 新增自由複習專用 `angle-near` 與 `hue-in-set` validator；未修改正式課程 validator。
- 更新自由複習入口期待值為七個已開放原理。
- 新增均衡／律動與統一／調和的直接回歸測試。

## 驗收狀態與已知事項

- 反覆、漸層、對稱是後續視覺與互動品質基準，不應被破壞。
- 均衡與律動經多輪人工回饋修正；目前實作及自動測試通過，仍應保留現有題目、validator 與成功動畫方向。
- 統一與調和已完成第一版實作及自動測試，下一步必須先人工操作與視覺驗收，不要直接擴充功能或重構。
- 尚未執行 browser 尺寸視覺驗證的結論性紀錄；接手者不可假裝已驗證 1366×768 或 1920×1080。
- Working tree 在本 checkpoint 前包含上述自由複習成果；checkpoint commit 後應保持乾淨。

## 測試基準

- 交接前完整執行：`node --test`
- 結果：413 tests / 413 pass / 0 fail。
- 修改自由複習時先跑直接相關測試，再跑 syntax check 與 `git diff --check`；只有明確要求或交接前才跑完整回歸。

## 下一步

1. 先人工驗收統一與調和六小題：題意、配色、互動、錯誤提示、每題動畫及完成按鈕。
2. 檢查 1366×768 與 1920×1080 的頁面密度、按鈕可見性及動畫是否維持完成構圖。
3. 若人工發現問題，只針對統一／調和做小幅修正並更新直接相關測試。
4. 未經使用者明確指示，不要開始對比、比例或單純，也不要改動正式三關、Mastery、Classroom Control 或已驗收原理。

## Git 安全規則

- 保留使用者修改；不得自行 `reset`、`checkout`、`restore` 或 `stash`。
- 修改前先檢查 `git status --short`。若有來源不明或不屬於當前任務的變更，先回報並停止。
- 不自動 push。commit 與 push 必須依使用者明確指示。
